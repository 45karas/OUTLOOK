services:
  - type: web
    name: outlook
    runtime: python
    plan: free
    buildCommand: pip install .
    startCommand: graphspy -i 0.0.0.0 -p $PORT -d database
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: XDG_DATA_HOME
        value: /opt/render/.local/share
